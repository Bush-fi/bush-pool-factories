{
  "name": "@bush.fi/v3-math-check",
  "version": "1.0.0",
  "description": "Generates bush-maths test data from the factories on a local chain, checks each factory's math-implementations against it, and runs the bush-maths suites",
  "license": "GPL-3.0-only",
  "private": true,
  "scripts": {
    "generate": "hardhat run src/run.ts",
    "check": "ts-node --transpile-only -r tsconfig-paths/register src/check.ts",
    "test:bush-maths": "npm run test:typescript && npm run test:python && npm run test:rust",
    "test:typescript": "cd ../../bush-maths/typescript && npm test",
    "test:python": "cd ../../bush-maths/python && python3 -m pytest -q",
    "test:rust": "cd ../../bush-maths/rust && cargo test --quiet",
    "all": "npm run generate && npm run check && npm run test:bush-maths"
  },
  "dependencies": {
    "@bush.fi/v3-common": "1.0.0",
    "@bush.fi/v3-deps": "1.0.0",
    "@bush.fi/v3-helpers": "1.0.0"
  }
}
